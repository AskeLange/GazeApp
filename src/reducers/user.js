

// Initial state
const init_state = {
  uid: null,
  token: null
};

// User reducer
export default (( state=init_state, action ) => {
  switch (action.type) {

    // Default
    default: return state;

  }
});